# Backend API base URL (must include /api prefix)
# Local: match backend PORT from locgiogiasi-be/.env (default in this repo: port 80)
NEXT_PUBLIC_API_BASE_URL=http://localhost:80/api

# Production example (set your deployed backend URL):
# NEXT_PUBLIC_API_BASE_URL=https://your-backend.up.railway.app/api
